# Get current FastF1 version

Gets the current installed FastF1 version available (via `reticulate`)
to the function. Displays a note if significantly out of date.

## Usage

``` r
get_fastf1_version()
```

## Value

version as class `package_version`
