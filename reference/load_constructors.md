# Load Constructor Info

Loads constructor info for all participants in a given season. Use
`.load_constructors()`for an uncached version of this function

## Usage

``` r
load_constructors()
```

## Value

A tibble with one row per constructor
